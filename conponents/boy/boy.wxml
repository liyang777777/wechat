<!-- 男生 -->
<view class="mans" >{{boy}}</view>
<view class="pai">
<view wx:for="{{male}}" wx:key="index" class="pan" bindtap="goTo" data-item="{{item}}" data-six="male" data-index="{{index}}">
<view>{{item.name}}</view>
<view class="color">{{item.bookCount}}本</view>
</view>
</view>
